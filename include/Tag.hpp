/* Tag.hpp
 * Header for the Tag class, which represents a tag and keeps track of the
 * files it is attached to.
 */

/* Stdlib includes */
#include <vector>

namespace tiger {
    class Tag {
        private:
            std::string name;
            std::vector<std::string> files;
        
        public:
    };
}
